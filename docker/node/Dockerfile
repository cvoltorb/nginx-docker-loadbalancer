# Выбираем образ для контейнера - соответсвенно образ Node последней версии
FROM node:latest

# Указываем параметры для Node
# Параметр production - для запуска и соотвественно порт 3003 на котором он работает
ENV NODE_ENV production
ENV LISTEN_PORT=3003

# С помощью команд ниже мы
# Создаем внутри контейнера папку кула скопируем наш app.js
RUN mkdir app
# Копируем наш app.js внутрь контейнера по данному пути
COPY ./docker/node/app/app.js /app/app.js
# Устанавливаем эту папку как рабочую
WORKDIR /app
# И подаем команды для запуска Node внутри контейнера
CMD [ "node", "app.js" ]
